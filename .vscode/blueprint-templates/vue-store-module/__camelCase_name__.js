import { defineStore } from 'pinia'

const state = () => ({})

const getters = {}


const actions = {}

export const use{{ pascalCase name }} = defineStore('{{ pascalCase name }}', {
  state,
  getters,
  actions
})
